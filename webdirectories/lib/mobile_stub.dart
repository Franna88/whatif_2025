// This is a stub file used on mobile platforms to provide empty implementations
// of web-specific functionality so the imports don't break on mobile.

// No-op implementation of runWebApp to satisfy import requirements
void runWebApp() {
  // This should never be called on mobile platforms
  throw UnsupportedError(
      'Web-specific functionality not available on mobile platforms');
}
